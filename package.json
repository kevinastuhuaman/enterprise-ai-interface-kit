{
  "name": "enterprise-ai-interface-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro check && astro build",
    "test": "astro check && playwright test && playwright test --config playwright.pages.config.ts",
    "validate:patterns": "node scripts/validate-patterns.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@axe-core/playwright": "^4.12.1",
    "@lucide/astro": "^1.24.0",
    "@playwright/test": "^1.61.1",
    "@types/node": "^24.13.3",
    "astro": "^7.0.7",
    "cross-env": "^10.1.0",
    "http-server": "^14.1.1",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
